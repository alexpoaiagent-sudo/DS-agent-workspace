# Правила Scout (обновляются автоматически через feedback loop)

## Начальные правила (v0, ручные)

1. **Различения формулируй как X ≠ Y** — с таблицей сравнения, не как описание. Пример: «Автономия ≠ Автоматизация» с колонками суть/вопрос/пример.

2. **SOTA должен быть конкретным** — не «AI развивается», а «Context Engineering (Write/Select/Compress/Isolate) заменяет prompt engineering как дисциплину» с источником и датой.

3. **Failure mode = конкретный сценарий** — не «может сломаться», а «агент без feedback loop деградирует в самосогласованный текст: внутренняя непротиворечивость растёт, но близость к реальности падает» (ref: DP.FM.005).

4. **Маршрутизация: домен → Pack, реализация → DS** — если находка про «как устроен мир» → Pack. Если про «какой инструмент использовать» → DS docs/.

5. **Не приносить новости** — «X выпустил Y» без методологического инсайта = мусор. Ценность = что можно **применить** или **формализовать**.

6. **Связывай с РП** — находка без связи с текущим РП или стратегическим фокусом = низкий приоритет. Исключение: breakthrough-идея для нового РП.

## Правила из feedback (2026-03-20, ревью #1)

7. **SOTA записывать как SOTA, не как метод** — если находка описывает тренд/состояние поля (Semantic Caching, vector DB), а не конкретную пошаговую процедуру → тип: SOTA-обновление, не метод. Методы = пошаговые алгоритмы (GEPA).

8. **Не приносить слишком общие «различения»** — «мультизадачность через ИИ-команды» — не различение, а наблюдение. Различение = чёткое X ≠ Y с таблицей. Если нет таблицы сравнения — это идея, не различение.

## Правила из feedback (2026-03-23, ревью #2)

9. **При повторной находке → обогащать, не дублировать** — если тема уже в accepted.md или PACK, не предлагать новый ID. Вместо этого: расширить существующую запись конкретикой (новые метрики, источники, примеры). Пример: GEPA уже AS.M.001 → добавить ICLR метрики в существующую запись.

10. **Проверять существующие файлы в PACK перед предложением ID** — перед «Capture: [название] → AS.M.XXX» → проверить, нет ли уже AS.M.YYY на эту тему в /04-methods/. Если есть — предлагать расширение, не новый файл.

11. **Различать платформенный SOTA (DP.*) vs метод агента (AS.M.*)** — Context Engineering, WISC, Prompt Chaining = платформенные паттерны → DP.SOTA.*. Конкретные алгоритмы для роли агента (GEPA, Trust Stack Design) → AS.M.*. Правило: если применимо ко всей платформе → DP, если специфика роли агента → AS.

12. **Failure modes с конкретикой = высокая ценность** — не «агент может ошибиться», а «Loop of Death: агент в рефлексии 3+ итераций с ошибкой → timeout / stale result. Severity: критический. Mitigation: max_iterations=3 + early exit». Формат: название, сценарий, severity, mitigation, пример.

## Правила из feedback (2026-03-24, ревью #3)

13. **Повторная тема = обогащение accepted, не новая находка** — если тема уже в accepted.md (даже с правкой) — НЕ выводить как TOP-N finding. Вместо этого: отдельная секция «Обогащения accepted» с delta (что нового). Считать в метрике отчёта как enrichments, не findings. Пример: WISC уже accepted #2 → новые данные по Anthropic thesis → enrichment #2, не finding #5.

